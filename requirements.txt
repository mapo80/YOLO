# Core
torch>=2.0
torchvision>=0.15
lightning>=2.0
jsonargparse[signatures]>=4.27.7

# Metrics
torchmetrics[detection]>=1.0
faster-coco-eval
pycocotools

# Config
omegaconf

# Utilities
einops
numpy<2.0
scipy>=1.10
Pillow
opencv-python

# Logging
loguru
rich
wandb
tensorboard

# Visualization
seaborn>=0.12
matplotlib>=3.5
graphviz

# HTTP requests (for weight download)
requests

# Export (ONNX)
onnx>=1.14.0
onnxsim>=0.4.0

# Export (TFLite) - optional, install separately if needed
# tensorflow>=2.13.0
# onnx2tf>=1.17.0

# Image Loaders (optional performance/encryption)
cryptography>=41.0.0  # For EncryptedImageLoader (AES-256)
PyTurboJPEG>=1.7.0    # For TurboJPEGLoader (requires libjpeg-turbo)
